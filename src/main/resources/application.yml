spring:
  application:
    name: PPG Hub

  # Database Configuration
  datasource:
    url: jdbc:postgresql://148.230.76.86:5433/ppg_hub
    username: postgres
    password: NIgBA3LQgmXTanJ8GjDewwOob5vxk8yJLkPgv1pa823mHhM9ovbtKFh4qMUP8WpF
    driver-class-name: org.postgresql.Driver

    # HikariCP Connection Pool Configuration
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: PPGHubHikariCP

  # JPA/Hibernate Configuration
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          time_zone: America/Sao_Paulo
        # Enable JSON type support
        types:
          print:
            banner: false
    show-sql: true
    open-in-view: false

  # Jackson Configuration
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false
    time-zone: America/Sao_Paulo
    default-property-inclusion: non_null

# Server Configuration
server:
  port: 8000
  servlet:
    context-path: /
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

# Logging Configuration
logging:
  level:
    root: INFO
    br.edu.ppg.hub: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  show-actuator: true

# Application Configuration
ppg:
  app:
    name: PPG System
    version: 0.1.0
    description: Sistema de Gestão para Programas de Pós-Graduação
  security:
    secret-key: sua_chave_secreta_muito_longa_e_segura_para_jwt
    token-expiration: 1800000 # 30 minutes in milliseconds
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:8080
