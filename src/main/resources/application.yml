spring:
  application:
    name: ppg-hub

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ppg_hub}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 60000ms

  cache:
    type: redis
    redis:
      time-to-live: 3600000 # 1 hora em ms
      cache-null-values: false

  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null

# Configurações da API
server:
  port: 8080
  servlet:
    context-path: /api
  compression:
    enabled: true
  error:
    include-message: always
    include-stacktrace: on_param

# Rate Limiting Configuration
rate-limit:
  requests-per-minute: ${RATE_LIMIT_RPM:100} # Limite de requisições por minuto por IP

# Resilience4j Circuit Breaker Configuration
resilience4j:
  circuitbreaker:
    instances:
      openalex:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 10  # Janela de 10 requisições
        minimum-number-of-calls: 5  # Mínimo de chamadas antes de calcular taxa de erro
        failure-rate-threshold: 50  # Abre circuito se 50% falharem
        wait-duration-in-open-state: 60s  # Aguarda 60s antes de tentar half-open
        permitted-number-of-calls-in-half-open-state: 3  # Permite 3 chamadas em half-open
        automatic-transition-from-open-to-half-open-enabled: true
        record-exceptions:
          - com.ppghub.domain.exception.OpenAlexException
          - org.springframework.web.reactive.function.client.WebClientResponseException
          - java.net.ConnectException
          - java.net.SocketTimeoutException

  retry:
    instances:
      openalex:
        max-attempts: 3
        wait-duration: 2s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
        retry-exceptions:
          - org.springframework.web.reactive.function.client.WebClientResponseException.ServiceUnavailable
          - org.springframework.web.reactive.function.client.WebClientResponseException.TooManyRequests
          - java.net.ConnectException

# OpenAlex Configuration
openalex:
  api:
    base-url: https://api.openalex.org
    email: ${OPENALEX_EMAIL:} # Polite pool email
    timeout: 30000
    max-retries: 3
    rate-limit:
      max-requests-per-day: 100000
      requests-per-second: 10

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true

# OpenAPI/Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  show-actuator: false

# Logging
logging:
  level:
    root: INFO
    com.ppghub: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
